export default function createIteratorObject(report) {
    const theEmployees = report.allEmployees.department
    for( const i of theEmployees ){
        let x = - 1
        return theEmployees[x];
    }
}